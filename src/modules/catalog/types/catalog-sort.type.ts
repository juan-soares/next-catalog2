/**
 * Define os tipos de ordenação disponíveis no catálogo.
 *
 * Este tipo representa regras de comportamento suportadas pelo domínio
 * de catálogo e não deve ser controlado pela URL ou pelo banco de dados.
 */

export type CatalogSort = "alph" | "updated" | "released";
