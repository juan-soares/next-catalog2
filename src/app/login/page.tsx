import { LoginForm } from "@/modules/user/features/auth";

export default function LoginPage() {
  return (
    <div>
      <h1>Login</h1>
      <main>
        <LoginForm />
      </main>
    </div>
  );
}
